
function addnumber(a, b)//a and b, represent the numbers to be added.
{ 
    return a+b; //return a + b; calculates the sum of a and b and returns the result.
}
let sum = addnumber(10, 5);
 console.log(sum);